# Discord Log Bot

Discord bot and sync service for logging and Google Sheet integration. See `ADD_CHANNEL_GUIDE.md`, `SETUP_SYNC_MENU.md`, and `RESTART_GUIDE.md` for setup.

In multi-stack deployment (e.g. `start-all.sh` in dev), this is **Stack 8**; sync API is exposed on host port **5020**.
